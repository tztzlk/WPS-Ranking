server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/profiledb
    username: youruser
    password: yourpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

security:
  jwt:
    secret: your_jwt_secret_key